import { CREATE_EVENT } from './eventConstants'

export function createEvent(event) {
  return {
    type: CREATE_EVENT,
    payload: event,
  }
}
export function createEvent(event) {
  return {
    type: CREATE_EVENT,
    payload: event,
  }
}
export function createEvent(event) {
  return {
    type: CREATE_EVENT,
    payload: event,
  }
}
